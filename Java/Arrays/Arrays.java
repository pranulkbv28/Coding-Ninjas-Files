package Arrays;

class Arrays {
    public static void main(String[] args) {
        // Decalring and Initialising a variable
        // int a;
        // a=10;
        // Declaring and Initialising an array;
        int arr[] = new int[10]; //only 10 or lesser number of elements can be stored here.
        arr = new int[6];   //this is the new length of the Array "arr"
        System.out.println(arr.length);
        System.out.println(arr[0]);
        // float []arr1;
        // double[] arr2;
    }    
}
